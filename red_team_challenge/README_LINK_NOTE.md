# Public challenge discovery note

The public red-team launch is intentionally self-contained under `red_team_challenge/`.

The root repository README remains unchanged in this release candidate to avoid replacing or compressing the broader RNKE documentation. The canonical challenge entry is [`README.md`](README.md), and PR #22 provides the repository-level launch record until a later minimal root-index edit is reviewed separately.
